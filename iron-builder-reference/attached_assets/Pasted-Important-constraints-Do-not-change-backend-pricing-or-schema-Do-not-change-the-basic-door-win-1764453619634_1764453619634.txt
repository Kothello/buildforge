Important constraints:

Do not change backend, pricing, or schema.

Do not change the basic door/window logic (placement rules, saving, etc.).

Only adjust the 3D highlight mesh used in Doors & Windows edit mode so it correctly matches wall heights on a single-slope main building, especially the high sidewall.

Context:

In the Red Iron Building Designer:

Roof Style = Single Slope.

In Doors & Windows, when I toggle Edit ON, each wall shows a blue highlight mesh representing the area where openings can be placed.

Current issue:

On the high side of a single-slope building, the highlight mesh height is only equal to the base wall height / low-side height.

It does not reach up to the actual high-side eave where the roof meets that wall.

Result: the selectable area looks too short and does not visually match the true wall height on the high side (see screenshots).

Goal:

On a single-slope main building, when Doors & Windows edit mode is ON:

The highlight mesh for the high sidewall must extend up to the high-side eave height, not just the base wall height.

The low side stays unchanged (up to the low eave).

Use the same eave height calculations already used for the roof geometry; do not invent new formulas.

Task:

Find where the door/window edit highlight meshes are created in the integrated configurator:

Likely in client/src/configurator/BuildingModel.tsx or a nearby helper that builds “interaction planes” for wall editing.

Identify the code that defines the wall highlight geometry (width, height, position) for each wall when edit mode is on.

Identify the existing eave height variables used for single-slope roofs:

There should already be values like lowSideEaveHeight, highSideEaveHeight, or equivalent, used to position the single-slope roof.

Confirm which one corresponds to the high side of the main building.

Fix the highlight height logic:

Currently, the highlight height appears to use base wall height (same for both sides).

Change the calculation so that:

If roof style is single-slope and the current wall is the high sidewall, set the highlight mesh height to the high-side eave height (the same value used by the roof geometry).

For the low sidewall, keep using low eave / base wall height as now.

For gable roofs or other roof styles, keep existing behavior unchanged.

Keep the footprint and door/window placement logic consistent:

Only extend the vertical dimension of the highlight mesh; do not change its X/Z footprint or how door/window coordinates are mapped to wall space.

The goal is purely to make the visual selection area match the actual physical wall height on that side.

Manual tests (describe and run):

In /builder:

Set a single-slope building (e.g., Width 40 ft, Length 60 ft, Height 12 ft, Roof Style = Single Slope).

Turn Doors & Windows Edit ON.

Rotate to view both low and high sidewalls.

Confirm:

Low side highlight reaches up to low eave (as before).

High side highlight now reaches the high eave, matching the roof edge with no extra gap.

Also verify on /sales/leads/:id with an existing single-slope lead that the same behavior appears in the embedded 3D configurator.

At the end, summarize:

The exact change you made to the highlight mesh height calculation (old expression vs new).

Which variables represent the high-side eave height, and how they are now used for the high-side wall highlight.

Confirmation from your manual tests that the high-wall edit mesh now visually reaches the eave correctly.
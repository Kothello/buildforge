Canonical building renderer from standalone builder:
configurator/iron-builder-studio-70695-74818-74577-9-62585/client/src/components/BuildingModel.tsx

CRM building renderer in use:
client/src/configurator/BuildingModel.tsx

I already copied iron-builder’s BuildingModel.tsx over once, but I still see two issues in the CRM app:

A visible gap where a gable lean-to roof meets the main roof (on sidewall gable connection).

In Doors & Windows edit mode, I cannot click all walls of a gable lean-to – some walls do not respond to clicks / do not highlight.

Goal:

Make the CRM BuildingModel.tsx lean-to behavior match the correct implementation so that:

Gable lean-to roofs tie into the main roof with no holes or gaps.

In edit mode, every existing wall of a gable lean-to has a visible mesh and a working onClick handler that calls onWallClick.

Task:

In the iron-builder file, locate the entire “Lean-To Structures” section:

The leanTos.map(...) block that computes:

effectiveWidth, effectiveLength, leanToHeight, leanToRoofRise, roofPanelLength, roofExtension (or similar).

lean-to roof meshes (left/right panels, gable panels, any hip pieces).

lean-to walls (side walls, end walls) and their onClick handlers for wall selection.

Locate the same block in the CRM file client/src/configurator/BuildingModel.tsx and do an actual diff between the two implementations.

Pay special attention to:

Any constants / offsets that control how the lean-to roof intersects the main roof (e.g. roofExtension, effectiveRoofRise, position y / z).

The meshes that represent gable lean-to walls, especially where you added CRM logic around gableAttachmentSide, enclosure, and leanTo.walls.

Fix roof gap at the gable lean-to / main roof intersection:

Adjust the lean-to roof panel geometry and positioning so that:

When a gable lean-to attaches to a sidewall, its roof plane either slightly overlaps or perfectly meets the main roof, with no visible gap along the join.

This holds for typical sizes (e.g. main: 50×60×16, lean-to: 20 ft width, 10 ft height) and the roof pitches you support.

Use the geometry from the iron-builder file as the reference; if the CRM version diverged (extra offsets or missing extension), bring it back in sync.

Run the CRM app and visually inspect a gable lean-to on a sidewall until the gap is gone.

Fix gable lean-to wall clickability:

In the gable lean-to section of BuildingModel.tsx, list all the meshes that represent lean-to walls:

local front, back, left, right walls in lean-to space.

For each wall that exists:

Ensure there is a mesh with an onClick handler that calls:
onWallClick(leanTo.wall, leanTo.id, <lean-to-local-wall-name>)
where <lean-to-local-wall-name> is 'front' | 'back' | 'left' | 'right' in lean-to local coordinates.

Ensure that shouldRenderWall(...) or any enclosure logic does not prevent the mesh from rendering when that wall is supposed to be present.

Confirm that all four lean-to walls that exist in the current configuration can be clicked in Doors & Windows edit mode and cause the highlight to move.

Preserve CRM-specific fields:

Keep handling of:

leanTo.gableAttachmentSide (attachment wall logic).

leanTo.enclosure and leanTo.walls in shouldRenderWall.

Integrate these with the corrected iron-builder geometry instead of removing them.

After editing, run manual tests and describe:

/builder:

Create a building with a gable lean-to on a sidewall. Confirm the roof join is solid (no gap) from several camera angles.

Turn Doors & Windows edit ON and click each lean-to wall; confirm highlight and click behavior for all walls.

/sales/leads/:id:

Open a lead with a gable lean-to and confirm the same behavior in the embedded configurator.

Finally, summarize:

Which exact parts of the leanTos.map block you changed in client/src/configurator/BuildingModel.tsx.

How you adjusted geometry to close the roof gap.

How you ensured every gable lean-to wall has a working onClick handler and is not filtered out by enclosure logic.
Paste this as your next Replit prompt. It keeps all logic the same, only restyles the PDF:

I want to upgrade the visual layout of the Quote PDF you just implemented. The data is correct, but the design is too basic.

HARD CONSTRAINTS – DO NOT VIOLATE

Do NOT modify:

client/src/configurator/**

configurator/iron-builder-studio-*/**

server/pricingEngine.ts

server/pricingRoutes.ts

Do NOT change any 3D builder behavior, camera, geometry, or pricing logic.

You may modify:

server/routes.ts – the /api/leads/:leadId/quotes/:quoteId/pdf endpoint

server/storage.ts – only if you need a slightly richer helper

This task is layout-only for the PDF. No behavior changes.

GOAL

Redesign the PDF generated by /api/leads/:leadId/quotes/:quoteId/pdf to look like a real, branded quote:

Header / letterhead

Quote metadata bar

Two-column summary (Customer + Building Specs)

Configuration table

Prominent total price panel

Small terms section at the bottom

Keep the same data, just change typography / positioning / grouping.

Layout spec (pdfkit)

In the PDF generation code:

Header / Brand

At top of page:

doc.fontSize(20).font("Helvetica-Bold").text("STEELFLOW ONE", { align: "left" })

doc.fontSize(12).font("Helvetica").text("Steel Building Quote", { align: "left" })

On the right side of the same line or just under it, add a small “company info” block:

Website: steelflow.one (or placeholder)

Email: sales@steelflow.one

Phone: (XXX) XXX-XXXX

Add a horizontal rule under the header using moveTo/lineTo/stroke.

Quote metadata bar

Immediately under the header, create a lightly boxed or visually grouped row for:

Quote ID (use the quote’s id or a formatted variant)

Quote Date (existing createdAt)

Valid Until (e.g. 30 days after createdAt)

Sales Rep (createdBy user name)

Example:

doc.fontSize(10).text("Quote #: " + quote.id, ...)

doc.text("Date: " + formattedDate, ...)

doc.text("Valid Until: " + formattedExpiration, ...)

doc.text("Sales Rep: " + (createdByName || "-"), ...)

Two-column summary (Customer + Building Specs)

Use two columns on the same vertical band:

Left: Customer

Company, Contact, Email, Phone

Right: Building Specifications

Width × Length × Height

Roof Style

Location / State if available

Use section titles with underline or bold:

doc.fontSize(12).font("Helvetica-Bold").text("Customer");
doc.moveDown(0.3);
doc.fontSize(10).font("Helvetica");
// customer fields...

// move to right column using doc.x / doc.y adjustments
doc.fontSize(12).font("Helvetica-Bold").text("Building Specifications", { continued: false });


Configuration table

Instead of just bullets, render a simple “table-like” section:

Headers: Category | Count

Rows:

Roll-up Doors

Personnel Doors

Windows

Lean-tos

Use the same aggregate counts you already compute (doors/windows/lean-tos).

You don’t need a perfect table layout engine, just consistent columns:

doc.moveDown();
doc.fontSize(12).font("Helvetica-Bold").text("Configuration");
doc.moveDown(0.3);
doc.fontSize(10).font("Helvetica");

const leftColX = doc.x;
const rightColX = leftColX + 200;

doc.text("Category", leftColX);
doc.text("Count", rightColX);
doc.moveDown(0.2);
doc.text("Roll-up Doors", leftColX);
doc.text(String(rollupDoorCount), rightColX);
// ... repeat for other categories


Total price panel

Near the bottom-right of the page, create a visually distinct block:

Header: “ESTIMATED PROJECT COST”

Big, bold price underneath.

doc.moveDown(1);
doc.fontSize(12).font("Helvetica-Bold").text("ESTIMATED PROJECT COST", { align: "right" });
doc.moveDown(0.3);
doc.fontSize(20).font("Helvetica-Bold").text(formattedPrice, { align: "right" });


You can optionally draw a light rectangle or line above it to separate from other content.

Terms & conditions

At the very bottom (small font), add a generic terms section such as:

doc.moveDown(1);
doc.fontSize(8).font("Helvetica");
doc.text(
  "Terms: Pricing is based on current steel market conditions and is subject to change. " +
  "Foundation, erection, permits, and taxes are not included unless explicitly stated. " +
  "Please review all specifications carefully before signing.",
  { align: "justify" }
);

Behavior / mechanics

Keep the buffer-based PDF response code you just implemented (collect chunks → Buffer.concat → send once).

Do not change:

The route path

Permissions logic

How the frontend triggers the download

Only modify PDF layout and text content/structure.

Final summary I expect

When you’re done, summarize:

The new high-level layout order of sections.

How you derived and formatted:

Quote ID

Valid Until date

Configuration counts for the table

Confirm that:

The PDF no longer looks like a plain debug dump.

The total price stands out visually.

No configurator/pricing logic was touched.

END PROMPT
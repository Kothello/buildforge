I moved this project from Lovable to Replit and the UI is now broken in production: the 3D model shows but all layout/styles look like plain HTML. In Lovable the UI is fully styled (panels, buttons, colors, etc.).

Act as a senior React/Vite/Tailwind engineer with full access to this Replit project. Do ALL of the following steps and fix the issue:

Open the project root and list all key files:

package.json

vite.config.*

tailwind.config.*

postcss.config.*

everything in src/ (especially main.tsx/main.jsx, App.*, and any index.css/globals.css/styles.css).

Verify that the main React entry file imports the global styles:

If src/main.tsx or src/main.jsx does NOT import the CSS, add the correct import, e.g.:
import "./index.css"; or whatever the main global CSS file is.

Ensure the import path matches the actual file name and location.

Verify that all styling files from the Lovable export exist here:

Any index.css / globals.css / App.css / component CSS modules.

If anything is missing, recreate/copy the file(s) in the same relative paths Lovable used and wire them up.

Check Tailwind/PostCSS configuration:

Ensure tailwind.config.* has the correct content globs pointing at ./index.html and ./src/**/*.{js,ts,jsx,tsx} (or similar).

Ensure postcss.config.* includes tailwindcss and autoprefixer.

Ensure the main CSS file includes the Tailwind directives:
@tailwind base;
@tailwind components;
@tailwind utilities;

Confirm the NPM scripts and build pipeline:

In package.json, verify "dev", "build", and "preview" scripts are valid for a Vite app.

Fix them if needed so:

npm run dev starts the dev server.

npm run build builds the app.

npm run preview serves the built app.

Update Replit run/deploy config so production uses the Vite build:

Build command: npm run build

Run command for production: npm run preview -- --host 0.0.0.0 --port 4173 (or the port Replit expects).

Make sure the static assets (including the generated CSS) are actually being served.

After changes, run locally inside Replit:

npm install (if needed)

npm run build

npm run preview

Confirm the UI in the preview matches the styled Lovable version (panels, fonts, colors, buttons, etc.).

Finally, show me:

The diff for every file you changed.

A short explanation of exactly what was wrong (missing CSS import, missing Tailwind config, wrong build command, etc.) and how you fixed it.

Execute these steps directly on the project files and then report back with the updated code and explanation.